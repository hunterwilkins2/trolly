apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trolly.fullname" . }}-test-connection"
  labels:
    {{- include "trolly.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trolly.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
